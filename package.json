{
  "name": "course-scheduler",
  "version": "1.0.0",
  "description": "University course scheduling application",
  "private": true,
  "scripts": {
    "setup": "npm run install:all && echo '✅ Setup complete! Run: npm run dev'",
    "install:all": "npm install && cd frontend && pnpm install && cd .. && task install",
    "dev": "concurrently -n backend,frontend -c blue,green \"task run\" \"cd frontend && pnpm dev\"",
    "dev:backend": "task run",
    "dev:frontend": "cd frontend && pnpm dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "task build",
    "build:frontend": "cd frontend && pnpm build",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "task test-unit",
    "test:frontend": "cd frontend && pnpm test",
    "db:migrate": "task migrate:up",
    "db:rollback": "task migrate:down",
    "db:new": "task schema-new --",
    "db:generate": "task jet-gen"
  },
  "keywords": [
    "scheduler",
    "university",
    "courses",
    "timetable"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "shadcn": "^3.6.2"
  }
}
